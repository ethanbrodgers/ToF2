import Navbar from '@/components/Navbar';

/**
 * The homepage for the website, used for navigation to other features on opening the site.
 */
export default function Homepage() {
    return ( <div>
        <Navbar />
        <p>This is the homepage</p>
    </div> );
}

